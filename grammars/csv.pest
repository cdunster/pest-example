WHITESPACE = _{ " " | "\t" }
NEWLINE = _{ "\r\n" | "\n" }
field = { (ASCII_DIGIT | "." | "-")+ }
record = { field ~ (("," ~ field)+ | ","?) }
record_list = _{ record ~ ((NEWLINE ~ record)+ | NEWLINE?) }
file = { SOI ~ record_list? ~ EOI }
